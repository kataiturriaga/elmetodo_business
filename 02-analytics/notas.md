# Notas a tener en cuenta

## Custom definitions

### program_type 
parametro del evento program_enroll

objetivo: saber cuantos enrolls hay a en base / completo
segmentar por usuario o user properties

is_guest

notifications_enabled

pedometer_enabled

has_active_program

days_since_last_training (vuestro bucket, perfecto)

training_level (si de verdad lo usáis para recomendar)

training_location (si de verdad lo usáis)

has_used_calculator (para activar ese feature)

platform (aunque GA4 ya tiene “device category/os”, pero si lo tenéis, ok)

program_goal

program_secondary_goal

program_first


